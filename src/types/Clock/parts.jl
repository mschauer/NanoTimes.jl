nanoseconds(x::Clock{T}) where T = value(x)
microseconds(x::Clock{T}) where T = fld(value(x), NANOSECONDS_PER_MICROSECOND)
milliseconds(x::Clock{T}) where T = fld(value(x), NANOSECONDS_PER_MILLISECOND)
seconds(x::Clock{T}) where T = fld(value(x), NANOSECONDS_PER_SECOND)
minutes(x::Clock{T}) where T = fld(value(x), NANOSECONDS_PER_MINUTE)
hours(x::Clock{T}) where T = fld(value(x), NANOSECONDS_PER_HOUR)
days(x::Clock{T}) where T = fld(value(x), NANOSECONDS_PER_DAY)
weeks(x::Clock{T}) where T = fld(value(x), NANOSECONDS_PER_WEEK)

Nanoseconds(x::Clock{T}) where T = Nanosecond(nanoseconds(x))
Microseconds(x::Clock{T}) where T = Microsecond(microseconds(x))
Milliseconds(x::Clock{T}) where T = Millisecond(milliseconds(x))
Seconds(x::Clock{T}) where T = Second(seconds(x))
Minutes(x::Clock{T}) where T = Minute(minutes(x))
Hours(x::Clock{T}) where T = Hour(hours(x))
Days(x::Clock{T}) where T = Day(days(x))
Weeks(x::Clock{T}) where T = Week(weeks(x))

nanosecond(x::Clock{T}) where T = mod(nanoseconds(x), NANOSECONDS_PER_MICROSECOND)
microsecond(x::Clock{T}) where T = mod(microseconds(x), MICROSECONDS_PER_MILLISECOND)
millisecond(x::Clock{T}) where T = mod(milliseconds(x), MILLISECONDS_PER_SECOND)
second(x::Clock{T}) where T = mod(seconds(x), SECONDS_PER_MINUTE)
minute(x::Clock{T}) where T = mod(minutes(x), MINUTES_PER_HOUR)
hour(x::Clock{T}) where T = mod(hours(x), HOURS_PER_DAY)
day(x::Clock{T}) where T = days(x)
week(x::Clock{T}) where T = weeks(x)

Nanosecond(x::Clock{T}) where T = Nanosecond(nanosecond(x))
Microsecond(x::Clock{T}) where T = Microsecond(microsecond(x))
Millisecond(x::Clock{T}) where T = Millisecond(millisecond(x))
Second(x::Clock{T}) where T = Second(second(x))
Minute(x::Clock{T}) where T = Minute(minute(x))
Hour(x::Clock{T}) where T = Hour(hour(x))
Day(x::Clock{T}) where T = Day(day(x))
Week(x::Clock{T}) where T = Week(week(x))
