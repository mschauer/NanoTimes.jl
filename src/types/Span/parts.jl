nanoseconds(x::Span{T}) where T = value(x)
microseconds(x::Span{T}) where T = fld(value(x), NANOSECONDS_PER_MICROSECOND)
milliseconds(x::Span{T}) where T = fld(value(x), NANOSECONDS_PER_MILLISECOND)
seconds(x::Span{T}) where T = fld(value(x), NANOSECONDS_PER_SECOND)
minutes(x::Span{T}) where T = fld(value(x), NANOSECONDS_PER_MINUTE)
hours(x::Span{T}) where T = fld(value(x), NANOSECONDS_PER_HOUR)
days(x::Span{T}) where T = fld(value(x), NANOSECONDS_PER_DAY)
weeks(x::Span{T}) where T = fld(value(x), NANOSECONDS_PER_WEEK)

Nanoseconds(x::Span{T}) where T = Nanosecond(nanoseconds(x))
Microseconds(x::Span{T}) where T = Microsecond(microseconds(x))
Milliseconds(x::Span{T}) where T = Millisecond(milliseconds(x))
Seconds(x::Span{T}) where T = Second(seconds(x))
Minutes(x::Span{T}) where T = Minute(minutes(x))
Hours(x::Span{T}) where T = Hour(hours(x))
Days(x::Span{T}) where T = Day(days(x))
Weeks(x::Span{T}) where T = Week(weeks(x))

nanosecond(x::Span{T}) where T = mod(nanoseconds(x), NANOSECONDS_PER_MICROSECOND)
microsecond(x::Span{T}) where T = mod(microseconds(x), MICROSECONDS_PER_MILLISECOND)
millisecond(x::Span{T}) where T = mod(milliseconds(x), MILLISECONDS_PER_SECOND)
second(x::Span{T}) where T = mod(seconds(x), SECONDS_PER_MINUTE)
minute(x::Span{T}) where T = mod(minutes(x), MINUTES_PER_HOUR)
hour(x::Span{T}) where T = mod(hours(x), HOURS_PER_DAY)
day(x::Span{T}) where T = days(x)
week(x::Span{T}) where T = weeks(x)

Nanosecond(x::Span{T}) where T = Nanosecond(nanosecond(x))
Microsecond(x::Span{T}) where T = Microsecond(microsecond(x))
Millisecond(x::Span{T}) where T = Millisecond(millisecond(x))
Second(x::Span{T}) where T = Second(second(x))
Minute(x::Span{T}) where T = Minute(minute(x))
Hour(x::Span{T}) where T = Hour(hour(x))
Day(x::Span{T}) where T = Day(day(x))
Week(x::Span{T}) where T = Week(week(x))
